# dependencies
node_modules

# env / secrets
.env
.env.local

# build output
dist

# OS artifacts
.DS_Store

# レビュー用 会話ログ（要配慮個人情報になり得るため絶対にコミットしない）
logs/

# 生成音声（TTS出力。会話内容を含むため絶対にコミットしない。自動削除あり）
public/audio/
